query CheckProduct ($id: ID!) {
  product: productsConnection(where: {id: $id}) {
    edges {
      node {
        id
        name
        description
        available
        price
        image {
          productImages {
            url
          }
        }
      }
    }
  }
}
